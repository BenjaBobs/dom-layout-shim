---
'dom-layout-shim': minor
---

Apply `rotate()`, `skew()`, and `matrix()` transforms to client geometry and precise polygonal hit testing. For example, `elementFromPoint()` no longer selects a rotated element through an empty corner of its bounding rectangle.
