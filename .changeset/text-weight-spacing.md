---
'dom-layout-shim': minor
---

Include inherited `font-weight` and `letter-spacing` in intrinsic and custom text measurement. A custom `textMeasurer` can now read `input.fontWeight` and `input.letterSpacing`, while the default measurer includes letter spacing in rendered widths.
