name: CSS support request
description: Request support for a CSS property, value, or interaction.
title: "css: "
labels:
  - enhancement
body:
  - type: textarea
    id: request
    attributes:
      label: CSS support request
      description: Describe the CSS feature, testing use case, minimal HTML and CSS, and expected Chromium behavior.
    validations:
      required: true
